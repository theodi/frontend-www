<head>
  <title>Open Data Institute | <%= strip_tags(yield :title).strip %><%= " | " + yield(:page_title) if content_for?(:page_title) %></title>
  <%= alternate_auto_discovery_link_tags %>
</head>

<body>
  
  <!-- wrapper div for slimmer content -->
  <div id='wrapper'>
    <%= yield %>
  </div>
  
</body>