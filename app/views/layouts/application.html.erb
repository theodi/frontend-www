<head>
  <%= metamagic :title => page_title,
    :og => {
      :title => page_title,
      :url => canonical_url,
      :description => "A page on the ODI website",
      :type => "website",
      :site_name => "Open Data Institute"
    },
    :twitter => {
      :card => "summary",
      :site => "@ODIHQ"
    }
  %>
  <link href="//maxcdn.bootstrapcdn.com/font-awesome/4.1.0/css/font-awesome.min.css" rel="stylesheet">
  <%= alternate_auto_discovery_link_tags %>
</head>

<body>

  <div id='wrapper'>
    <% if content_for?(:title) %>
      <header class="page-title">
          <div class="container header-wrap">
              <h1><%= yield :title %></h1>
              <% if content_for?(:page_title_extra) %>
                  <%= yield :page_title_extra %>
              <% end %>
          </div>
      </header>
    <% end %>

    <%= yield :hero %>

    <div class="container">
        <div class="row">
          <%= yield %>
        </div>
    </div>

  </div>

  <%= yield :extra_javascript %>

</body>
